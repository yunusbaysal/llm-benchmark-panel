# Copy to .env and fill in what you use. Nothing here is required to run
# the demo — mock: providers need no key at all.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Optional: model used as the LLM-judge scorer for subjective tasks.
# Format is "<provider>:<model>", same as any other model id below.
JUDGE_MODEL=
